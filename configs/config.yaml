app:
  name: AgentHub
  env: development

server:
  host: 127.0.0.1
  port: 8848

log:
  level: info
  format: text

agent:
  max_steps: 4
  memory:
    type: sliding
    max_messages: 20
    token_budget: 4096
