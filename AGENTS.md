# AgentHub 项目协作指南

## 项目目标

通过持续实现 AgentHub，学习企业级 Go 服务与 AI Agent 系统设计。课程路线和进度以 `AGENTHUB_PLAN.md` 为准。

## 工作方式

- 每次只推进一个可运行、可验证的小目标。
- 用户可见价值优先：每节开始前必须明确“改动前看不到什么、完成后能看到什么”；如果不能产生新的可观察行为，也不解决已复现的问题，默认不作为当前课程。
- 不为完整性预先堆叠抽象、状态、配置、重试或诊断能力。基础设施工作只有在阻塞下一个用户可见目标，或已有真实失败证据时才推进；否则记录边界并延后。
- 每新增一层封装，必须指出它消除的具体重复、冲突、生命周期问题或外部依赖边界；说不清就使用更直接的实现，已有但未证明价值的封装允许在保持行为的前提下简化。
- 课程小节拆成连续的小阶段；每次只讲一个阶段，先解释它解决的问题、调用方、状态或数据流及设计原因，再说明修改文件、具体位置和代码写法。
- 阶段讲解结束时，明确本阶段完成后得到什么，以及下一阶段将解决什么；不要一次倾倒后续所有阶段的实现清单。
- 核心代码对状态语义、资源生命周期、并发约束和错误归属等非显然设计保留简洁注释；不为明显语句逐行加注释，也不为单次课程规则额外创建文档或 Skill。
- 先解释目标、设计和关键语法，再由学习者编写核心代码。
- 除非明确要求，不直接完成核心实现。
- 出现报错时先定位当前问题，不顺带重构无关代码。
- 不在仓库中记录对话内容、个人资料或能力画像。

## 课程完成条件

一节课只有同时满足以下条件才算完成：

1. 代码已实现。
2. 格式化、测试、静态检查或构建中的适用项已通过。
3. 学习者能解释本节关键概念。
4. `AGENTHUB_PLAN.md` 已更新状态、验收记录和下一步。
5. 已按仓库内 `.agents/skills/agenthub-learning-checkpoint/SKILL.md` 执行 Git 检查点流程。

## Git 规则

- 使用 Conventional Commits。
- 每节课验收通过并更新学习计划后，自动暂存本节相关文件并创建提交。
- 提交前检查暂存范围，只包含当前课程的单一主题；无关改动留在工作区。
- 不自动执行 `git push`。
- 提交前检查 `.env`、密钥、令牌和个人信息；发现风险时停止提交并说明原因。

## 常用命令

```bash
go fmt ./...
go test ./...
go vet ./...
go build ./...
go run ./cmd/agenthub
```

按当前课程选择最小且有效的验证命令；不要为了形式运行无关检查。

## 学习图示

- 对架构、调用链、状态流转或关键概念有持续学习价值的生成图，应保存到当前仓库，不只保留在对话中。
- 通用图示优先放入 `docs/images/`；与单个包紧密关联的图示可以放在对应包目录。
- 使用能表达内容的稳定文件名；避免 `image.png`、`exported_image.png` 等无语义名称。
- 只保存有长期参考价值的最终图，不提交临时截图、重复版本或仅用于展示的装饰图。
- 保存后检查图片可正常打开、内容与当前实现一致，并在课程检查点中作为相关学习资产单独核对和提交。

## 项目约定

- 正式应用入口：`cmd/agenthub/`，后续生产功能统一使用 Eino。
- 教学对照入口：`examples/`，不得继续承载新的生产功能。
- 私有生产代码：`internal/`；当前包含会话上下文等 AgentHub 应用逻辑。
- 历史自研 Runtime：`examples/selfbuilt-runtime/internal/`，仅供 Phase A 教学对照；生产代码不得依赖或继续扩展。
- 生产配置文件按具体阶段放在 `configs/`；教学示例专用配置与示例放在同一目录。
- 项目脚本：`scripts/`
- 项目文档：`docs/`
- 学习计划：`AGENTHUB_PLAN.md`
- 项目级 Skills：`.agents/skills/`
- 敏感配置不得提交；`.env` 必须保持在 `.gitignore` 中。
